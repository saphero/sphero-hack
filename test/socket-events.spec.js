'use strict';

/* eslint-disable */

const expect = require('chai').expect;
require(__dirname + '/../server');

describe('socket listener tests', () => {
  it('orb should recieve connect command', () => {

  });
});
